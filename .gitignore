# --- secrets -------------------------------------------------------------
public_html/includes/config.php
.env
.env.*

# --- local database / installer state -----------------------------------
storage/
public_html/install/installed.lock
*.sqlite
*.sqlite-journal

# --- dependencies & tooling ----------------------------------------------
node_modules/
vendor/
.DS_Store
Thumbs.db

# --- editors --------------------------------------------------------------
.idea/
.vscode/
*.swp

# --- release bundle (regenerate with tools/pack.sh) -----------------------
jollofliving-hostgator.zip
jollof-update.zip

# The incremental update zip is published under dist/ for download.
!dist/jollof-update.zip

# --- Android app ----------------------------------------------------------
# Build output and generated bundles. The source in mobile/src and the
# Android project in mobile/android are committed; these are not.
mobile/www/
mobile/www-test/
mobile/android/build/
mobile/android/app/build/
mobile/android/.gradle/
mobile/android/local.properties
mobile/android/app/src/main/assets/public/
mobile/android/capacitor-cordova-android-plugins/
*.apk
*.aab
*.keystore
*.jks
